name: akash-wf2

on:
  push:
    branches: ["main"]

jobs:
  second_job:
    runs-on: ubuntu-latest
  
  steps:
  - uses: actions/checkout@v3

  - name: trying to create second workflow
  run: echo Second workflow teyar krr rhe h

  

  